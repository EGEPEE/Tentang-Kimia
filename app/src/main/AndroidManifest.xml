<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="egaprasetianti.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/imgsplash"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ModuleActivity" />
        <activity android:name=".UnsurActivity" />
        <activity android:name=".SpsActivity" />
        <activity android:name=".PerkembanganActivity"></activity>
    </application>

</manifest>